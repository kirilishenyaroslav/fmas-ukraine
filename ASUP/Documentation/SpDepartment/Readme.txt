—правочник подразделений, верси€ от 02.06.2005

Bpl - пакет самого справочника
Example - пример использовани€ (подправить ini-шку)
Need_Bpl - нужные bpl'и (кроме CX, FIBPlus7 и стандартных)
Source - исходники

¬ходные параметры:
100% об€зательным €вл€етс€ Handle к базе данных, остальные параметры
можно не задавать, у них есть разумные умолчани€.

 - DBHandle
	Handle от pFIBDatabase
 - ShowStyle
	“ип показа справочника(1 - MDI, 0 - Modal).
	ѕо умолчанию показ модальный
 - Select
	“ип выборки (0 - без выборки, 1 - выборка одного, 2 - множественный).
	ѕо умолчанию выбор одного подразделени€.
 - Actual_Date
	ƒата, на которую просматриваютс€ подразделени€.
	ѕо умолчанию - текуща€ дата на клиенте.
 - Root_Department
	»дентификатор самого верхнего подразделени€, корн€ той ветки, котора€ будет
	выводитс€. –екомендуетс€ не заполн€ть это поле, тогда будет выбрано все
	предпри€тие (его идентификатор хранитс€ в таблице Ini_Asup_Consts, поле
	Local_Department). ≈сли вам нужна лишь часть подразделений (допустим,
	только факультеты), найдите идентификатор вышесто€щего подразделени€
	и засылайте его сюда.
 - AllowEdit     (true/false)
	–азрешаетс€ ли редактирование справочника.
	ѕо умолчанию запрещено (AllowEdit = false)
 - Id_Property
	»дентификатор свойства, по которому будут отбиратьс€ подразделени€. »з всей
	ветки будут отобраны лишь те подразделени€, у которых установлено это свойство.
	ѕо умолчанию будет использовано свойство 1, которое есть у всех подразделений.


¬ыходные параметры:
 - Id_Department - идентификатор подразделени€
 - Name_Short - сокращенное название подразделени€
 - Name_Full - полное название подразделени€.
–екомендуетс€ использовать полное название подразделени€.